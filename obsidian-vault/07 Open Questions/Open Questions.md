---
tags:
  - questions
  - planning
status: active
---

# Open Questions

## Phase 1

- What exact shape should `ContextMatrix` have in TypeScript?
- Should clarification-only packages reuse the current package schema or introduce a new package kind?
- How should approved assumptions be represented in user-facing CLI/MCP flows?
- Which existing contract tests must change first when `readyForFrontendAgent` becomes lifecycle-gated?

## Later Phases

- How should specialist agents be invoked in Codex and Claude Code?
- How strict should pixel-perfect QA be before a real visual reference exists?
- Should the QA team generate screenshots into the package or target repo?

